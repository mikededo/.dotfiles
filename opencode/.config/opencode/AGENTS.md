# Global Rules

## Communication

Ponytail mode always. Prefer `/ponytail ultra` for pragmatic minimal code.

## Version Control

Use jj over git. Default all VCS ops to jj. Fallback git only if jj absent.
